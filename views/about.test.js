import { test, expect, describe } from "@jest/globals";
import { whispersCount } from "../controllers/whispersController.js";

describe("about", () => {
  test("should return the sum of two numbers", () => {
    expect(whispersCount).not.toBe(0);
  });
});
